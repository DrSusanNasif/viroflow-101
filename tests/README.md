# tests

This folder will store simple tests for the project.

Examples:
- Test FASTA files
- Expected output files
- Commands to check that scripts run correctly

Goal:
Make sure the workflow still works after changes.
